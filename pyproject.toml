[project]
name = "zprp-23z-python-orm"
version = "0.1.0"
description = "Python ORM"
authors = [
    {name = "xDepcio", email = "olek.drwal@gmail.com"},
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
